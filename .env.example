# Required
OPERATOR_PASSWORD=changeme

# Optional
OPERATOR_NAME=admin
IRC_PORT=6667
UPDATE_CHECK_INTERVAL=3600   # How often to check for updates (seconds)
ROLLING_GRACE_PERIOD=90      # Docker stop timeout per node (pircd migrates users in ~10s)
INTER_NODE_PAUSE=60          # Seconds to wait between updating each node
